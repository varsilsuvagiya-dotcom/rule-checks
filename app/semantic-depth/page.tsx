export default function SemanticDepthPage() {
  return (
    <main>
      <p style={{ fontWeight: 700, color: '#b00020' }}>
        TEST FIXTURE — intentionally broken, not a real business.
      </p>

      <h1>Broken Fixture Co</h1>

      <p>
        Broken Fixture Co sells storage. Storage is good. Storage helps you store things. We store storage
        so your storage is stored well. Our storage team stores storage every day. Storage keeps your
        storage safe. Storage is stored in storage. Storage users like storage because storage works.
      </p>

      <h2>Storage Plans</h2>
      <p>
        We have storage plans. Storage plans store storage. Pick a storage plan. Storage plans come with
        storage. Every storage plan includes storage. Storage plans are for storage. Choose the storage
        plan that stores your storage.
      </p>

      <h2>Storage Pricing</h2>
      <p>
        Storage pricing covers storage. Storage costs money. Storage pricing is priced for storage. Storage
        pricing depends on storage. Storage pricing changes with storage. Storage pricing is simple storage
        pricing. Storage pricing helps storage users pay for storage.
      </p>

      <h2>Storage Support</h2>
      <p>
        Storage support supports storage. Contact storage support about storage. Storage support helps
        storage. Storage support is available for storage. Storage support answers storage questions about
        storage. Storage support cares about storage.
      </p>

      <p>
        Storage is storage. Storage stores storage for storage users who need storage. Storage matters.
        Storage is what we do. Storage is our storage business. Storage keeps storage users happy with
        storage. Storage is storage every single day. Storage is storage, and storage will keep being
        storage. Storage users trust storage. Storage is the storage we provide. Storage, storage, storage.
        Storage users choose storage because storage is storage they can rely on. Storage stays storage no
        matter how much storage you store. Storage is offered as storage, sold as storage, and delivered
        as storage. Storage will always be storage at Broken Fixture Co, where storage is simply storage.
      </p>
    </main>
  );
}
